# Copy this file to .env and fill in your Anthropic API key.
# WARNING: VITE_* variables are embedded in the client bundle — visible to end users.
# For production, use a Cloudflare Worker proxy instead.
VITE_ANTHROPIC_API_KEY=sk-ant-...
